apiVersion: nais.io/v1alpha1
kind: Application

metadata:
  labels:
    team: tpts
  name: tiltakspenger-meldekort
  namespace: tpts

spec:
  ingresses:
    - {{ ingressOrigin }}{{ ingressPath }}
  image: {{ image }}
  port: 3050
  # Prober følger felles mal, se «Delte konvensjoner» i navikt/tiltakspenger AGENTS.md. Avvik skal begrunnes med kommentar her.
  startup:
    path: {{ ingressPath }}/internal/isAlive
    initialDelay: 5
    periodSeconds: 5
    failureThreshold: 60
    timeout: 3
  liveness:
    path: {{ ingressPath }}/internal/isAlive
    periodSeconds: 10
    failureThreshold: 3
    timeout: 3
  readiness:
    path: {{ ingressPath }}/internal/isReady
    periodSeconds: 5
    failureThreshold: 1
    timeout: 3
  env:
  {{#each env}}
    - name: {{name}}
      value: {{value}}
  {{/each}}
  accessPolicy:
    outbound:
      rules:
        - application: nav-dekoratoren
          namespace: personbruker
        - application: tiltakspenger-meldekort-api
  replicas:
  {{#with replicas}}
    min: {{ replicas.min }}
    max: {{ replicas.max }}
  {{/with}}
  observability:
    autoInstrumentation:
      enabled: true
      runtime: nodejs
    logging:
      destinations:
        - id: elastic
        - id: loki
  idporten:
    enabled: true
    sidecar:
      enabled: true
      autoLogin: true
      autoLoginIgnorePaths:
        - /tiltakspenger/meldekort/demo/**
      level: idporten-loa-substantial
  tokenx:
    enabled: true
